<!-- show.html.erb -->
<% content_for :page_header do %>
  <div class="page-header">
    <h1 class="page-header-title">Condition Report</h1>
    <h4 class="page-header-address">Level 1, 41-43 Stewart St, Richmond, VIC 3121, Australia</h4>
  </div>
<!--   <div class="progress">
    <div class="progress-bar" role="progressbar" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100" style="width: 20%">
      20%
    </div>
  </div> -->
<% end %>
<!--  -->
<div class="container">
  <div class="room">
    <div class="row">

      <div class="col-md-12">
        <% @rooms.each do |room| %>
          <h3><%= room.name %></h3>
          <% room.room_elements.each do |element| %>
            <div class="panel">
              <div class="panel-heading">
                <h4 class="panel-title"> <%= element.name %> </h4>
              </div>
              <div class="panel-body">
                Comments 💬
                <% element.comments.each do |comment| %>
                    <%= comment.text_comment %>
                    <%= render partial: "shared/lightbox_image", locals: {photo: comment.photo} %> <br>
                    <p> Current State: <%= comment.state %></p>
                <% end %>
              </div>
            </div>
          <% end %>
        <% end %>



      <div class="action-bar">
        <%= link_to "Confirm", confirmation_url, class: "btn btn-primary action-bar-button"%>
        <div class="action-bar-light-part"></div>
      </div>
    </div>
  </div>
</div>





